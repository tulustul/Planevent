from planevent.settings.shared import *

INI_FILE = 'development.ini'

APP_URL = 'http://localhost:8080'

REDIS = {
    'URL': 'localhost',
    'PORT': 6379,
    'CACHE_DB': 0,
    'DB': 1,
    'PASSWORD': '',
}
