from planevent.settings.shared import *

INI_FILE = 'testing.ini'

APP_URL = 'http://localhost:8080'

REDIS = {
    'URL': 'localhost',
    'PORT': 6379,
    'CACHE_DB': 0,
    'DB': 15,
    'PASSWORD': '',
}
